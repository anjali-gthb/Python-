# two inputs in a same line .
# using split()func
# it is important to add space by the user between input in name and age.
# name,age=input("enter your name and age is separated by space ").split();
# print(name+" "+age)

# we can change the space with , also
name,age=input("enter your name and age is separated by comma ").split(",");
print(name+" "+age)