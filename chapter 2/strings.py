# collection of characters inside double or sing quotes.

first_name="anjali";
second_name="hi"
fullname=first_name + second_name;    #concatenate two strings.
fullname=first_name +" "+second_name;

# we cannot concatenate a string with any other integer .
# a string will concatenate with a string only.

# print(first_name+32); #it will give me an error:type error.
# print(first_name + str(32)); #str()func will convert that integer into a string


# you can use multiply operator with string
print(first_name * 2);
